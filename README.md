# Salary_Prediction

A simple Python + Streamlit app to predict salaries using a trained machine learning model.

How to Run
pip install -r requirements.txt
streamlit run app_ui.py

Enter your inputs in the web app to get the predicted salary.
